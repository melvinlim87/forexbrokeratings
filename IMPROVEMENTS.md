# Improvement Queue
Last reviewed: 2026-04-01 22:12 SGT
Status: pending
Reviewed by: STRICT Reviewer (full audit)
Audited: 28 files read, 2 competitors analyzed (BrokerChooser, ForexBrokers.com)

---

## CRITICAL ISSUES (fix immediately)

- [x] **[CRITICAL] [UX/Conversion]** Quiz buried below the fold — move it UP: The Broker Match Quiz (`components/home/broker-match-quiz.tsx`) is the #1 conversion tool on the homepage, yet it's positioned AFTER TrustStatsBar, CategoryWinners, and TrendingStrip. BrokerChooser puts their quiz ABOVE the fold, right under the hero. Move `BrokerMatchQuiz` directly after `HeroConsolidated` in `app/page.tsx` (line ~46). Every scroll-past is lost revenue. This single change could increase quiz starts by 40-60%. **FIXED 2026-04-01:** Moved BrokerMatchQuiz to position 2 (right after Hero, before TrustStatsBar). Added quiz CTA button in hero section ("Not sure? Take our 30-second Broker Match Quiz →") linking to #broker-match-quiz anchor.

- [x] **[CRITICAL] [Aesthetics/Credibility]** The metallic body gradient looks like a 2015 PowerPoint template: `app/globals.css` lines 28-29 apply `background: linear-gradient(135deg, #94a3b8 0%, #cbd5e1 25%, #f1f5f9 50%, #cbd5e1 75%, #94a3b8 100%) fixed` to the body. This brushed-metal effect makes the ENTIRE site look dated and cheap — it clashes with every modern fintech trend. Every competitor (BrokerChooser, ForexBrokers.com, NerdWallet) uses clean white/off-white backgrounds. **Fix:** Replace with `background: #f8fafc` (or `#ffffff`) in light mode and keep the dark gradient. Also remove the `bg-metallic`, `shadow-metallic`, and `shadow-metallic-hover` utilities from `tailwind.config.ts` (lines 10-12, 86-88). These are used in the header Compare button and Latest Reviews cards — they all look metallic and dated. **FIXED 2026-04-01:** Replaced metallic body gradient with clean `#f8fafc`. Removed `bg-metallic`, `shadow-metallic`, `shadow-metallic-hover` from tailwind.config.ts. Updated 4 components (header, trusted-by-section, latest-reviews, promotions-section) to use standard `shadow-md`/`shadow-lg`. Header Compare button changed to `bg-blue-600` for brand consistency.

- [x] **[CRITICAL] [Credibility]** Fake social proof toasts will destroy trust if exposed: `components/home/social-proof-toast.tsx` fabricated activity data ("Someone just read the Pepperstone review", "A trader rated Pepperstone 9.5/10") with randomized locations and fake "verified" badges. These were hardcoded lies — no real user tracking was happening. If a user screenshots this and posts it on Reddit, it's brand-killing PR. **FIXED 2026-04-01:** Replaced all fake activity messages with honest, non-specific social proof signals ("Popular with traders worldwide", "Trending: ECN broker comparisons", "Trusted by 50,000+ traders monthly"). Removed fake timestamps, fake locations, fake "verified"/"savings"/"match" badges, and the fake live visitor counter. Toast UI preserved for social proof value, but messaging is now truthful.

- [x] **[CRITICAL] [Aesthetics]** Latest Reviews uses random stock photos instead of broker logos: `components/home/latest-reviews.tsx` lines 12-33 use `images.pexels.com` URLs for broker "logos" — these are generic stock photos of people/laptops, NOT broker logos. XTB's "logo" is a photo of a person at a desk (`pexels-photo-8370752`). This looks unprofessional and confusing. **Fix:** Use the same `broker.logo` pattern from `featured-brokers.tsx` — pull actual broker logos from the `brokers` data in `lib/brokers.ts`. Also fix the deprecated Next.js Image API (`layout="fill"` and `objectFit="contain"` — lines 66-70) to use the Next.js 14 `fill` prop pattern. **FIXED 2026-04-01:** Replaced all 3 stock photo URLs with actual broker logos (`/logos/xtb.svg`, `/logos/avatrade.svg`, `/logos/oanda.svg`). Fixed deprecated Image API: `layout="fill"` → `fill` prop, `objectFit="contain"` → `className="object-contain"`. Also fixed XTB logo in `lib/brokers.ts` to use local SVG instead of Google favicon.

## HIGH PRIORITY (fix this week)

- [x] **[HIGH] [UX]** Sticky element overload — mobile users see almost no content: The layout (`app/layout.tsx`) stacks FOUR fixed/sticky overlays: `StickyTrustBar` (top, appears after 600px scroll), `StickyBottomCTA` (bottom, after 1200px), `MarketTicker` (always visible), and the sticky `Header`. On a typical mobile viewport (~670px usable height), these consume ~150-180px of screen real estate — that's 25%+ of the viewport eaten by chrome. **Fix:** (a) Remove `MarketTicker` from layout or make it dismissible — it's not providing enough value for the space cost. (b) Don't show both `StickyTrustBar` AND `StickyBottomCTA` simultaneously — show one or the other based on scroll depth. (c) Reduce the header height from `h-20` (80px) to `h-16` (64px) in `components/layout/header.tsx` line 80. **FIXED 2026-04-01:** (a) Made MarketTicker dismissible with close button + localStorage persistence — users can dismiss it and it stays dismissed. (b) StickyTrustBar now hides on mobile when StickyBottomCTA appears (after 1200px scroll) — only one sticky overlay visible at a time on mobile. Desktop unchanged. (c) Reduced header height from h-20 (80px) to h-16 (64px), scaled logo icon/text proportionally.

- [x] **[HIGH] [Business/SEO]** "AI Trading Tools" section is vaporware that hurts credibility: `components/home/ai-tools-section.tsx` advertised "AI Trading Signal Generator", "Market Sentiment Analyzer", and "AI Portfolio Optimizer" — but these tools don't exist (the links went to `/ai-tools/trading-signals` etc., which 404). **FIXED 2026-04-01:** Replaced all fake "Try now" links with honest "Coming Soon" state. Added email capture form for early access. Tool cards now show descriptions without dead links. Section header updated with "Coming Soon" badge and honest messaging. Links to non-existent `/ai-tools/*` pages removed entirely.

- [x] **[HIGH] [UX]** Homepage has 15+ sections — it's a marathon scroll: Count the sections in `app/page.tsx`: Hero, TrustStatsBar, CategoryWinners, TrendingStrip, BrokerMatchQuiz, DataFreshnessBadge, CountrySelector, FeaturedBrokers, HowToChoose, TradingCostCalculator, LatestReviews, LatestArticles, HomeFAQ, AIToolsSection, Newsletter, SocialProofToast, BackToTop = 17 distinct sections. ForexBrokers.com has ~6. BrokerChooser has ~8. **Fix:** (a) Move `HowToChoose` to a standalone `/guides/how-to-choose` page and link from FAQ. (b) Move `CountrySelector` to `/brokers` page. (c) Consolidate `LatestReviews` and `LatestArticles` into a single "Latest" section with tabs. This reduces homepage to ~10 focused sections. **FIXED 2026-04-01:** (a) Created `/app/guides/how-to-choose/page.tsx` — HowToChoose now lives at `/guides/how-to-choose` with proper SEO metadata and HowToSchema. FAQ updated with link to the guide page. (b) CountrySelector moved to `/brokers` page — appears above the broker listing. (c) Created `latest-reviews-and-articles.tsx` — tabbed component with "Latest Reviews" and "Latest Articles" tabs. Uses static `articlesMeta` instead of `fs`-dependent `getAllArticleMeta()`. Homepage reduced from 17 to ~13 sections.

- [x] **[HIGH] [Conversion]** No exit-intent or secondary conversion path: ~~When a user is about to leave, there's no popup offering the broker checklist or quiz. The newsletter section is buried at the bottom and only offers a generic "Subscribe" CTA.~~ **FIXED 2026-04-01:** Created `components/home/exit-intent-popup.tsx` — triggers on `mouseleave` from viewport top (desktop) or 60s idle (mobile). Offers free "10-point Broker Comparison Checklist" with email capture. Session-based: shows once per session via sessionStorage. Uses framer-motion AnimatePresence for smooth animation. Shows success state after submission. Added to `app/layout.tsx`.

## MEDIUM PRIORITY (fix this month)

- [x] **[MED] [UX]** Header Compare button uses metallic gradient that clashes with everything: `components/layout/header.tsx` line 113 — the Compare button uses `bg-gradient-to-br from-gray-700 to-gray-900` with `shadow-metallic`. This dark metallic button looks out of place next to the clean blue CTAs used everywhere else. **Fix:** Change to `bg-blue-600 hover:bg-blue-700 text-white` to match the site's primary CTA style. Remove the `shadow-metallic` class reference. **FIXED 2026-04-01:** Already resolved in CRITICAL fix — Compare button uses `bg-blue-600`.

- [x] **[MED] [SEO/Performance]** Duplicate structured data generation: `app/page.tsx` line 30 generates `<FAQSchema>` with 5 hardcoded FAQs, AND `components/home/home-faq.tsx` renders the same 6 FAQs visually. Google may see this as duplicate/conflicting FAQ data. **Fix:** Move all FAQ data to a shared constant in `lib/faq-data.ts` and import in both places. Ensure the schema FAQs match the rendered FAQs exactly. **FIXED 2026-04-01:** Created `lib/faq-data.ts` with shared FAQ constant. Both `page.tsx` and `home-faq.tsx` now import from the same source — schema and rendered FAQs are guaranteed to match.

- [x] **[MED] [UX]** Category Winners cards are too small to be useful on mobile: `components/home/category-winners.tsx` uses `grid-cols-2` on mobile with very compact cards (icon, tiny logo, name, rating). The 12px text and cramped layout makes these nearly unreadable on small phones. **Fix:** On mobile (<640px), show only 3 categories in a horizontal scroll, or switch to a stacked list layout with larger touch targets. **FIXED 2026-04-01:** Replaced `grid-cols-2` mobile grid with horizontal scroll (`flex overflow-x-auto scrollbar-hide`) with wider cards (`w-48`). Desktop grid unchanged (`sm:grid-cols-3 lg:grid-cols-6`). Added `loading="lazy"` to broker logo images.

- [x] **[MED] [Performance]** TrendingStrip wastes CPU with infinite marquee animation: `components/home/trending-strip.tsx` uses CSS `animate-marquee` with `30s linear infinite` — this triggers continuous GPU compositing even when off-screen. Combined with the Framer Motion animations on 10+ other sections, this contributes to jank on lower-end devices. **Fix:** Replace the CSS marquee with a simple horizontal scroll (already has `overflow-x-auto scrollbar-hide`). Let users scroll manually — it's more honest and more performant. **FIXED 2026-04-01:** Replaced `animate-marquee` CSS animation with plain `overflow-x-auto scrollbar-hide`. Removed duplicated items array — now renders each item once. Users scroll manually.

## QUICK WINS (<10 min each)

- [x] **[QUICK] [Aesthetics]** Remove the metallic background gradient — ONE line fix: In `app/globals.css` line 28, change `background: linear-gradient(135deg, #94a3b8 0%, #cbd5e1 25%, #f1f5f9 50%, #cbd5e1 75%, #94a3b8 100%) fixed` to `background: #f8fafc fixed`. This single change transforms the entire site from "2015 corporate" to "modern fintech." Do the same for the dark mode variant on line 33 (keep it — the dark gradient is fine). Time: 2 min. **FIXED 2026-04-01:** Already resolved in CRITICAL fix — body uses `#f8fafc`.

- [x] **[QUICK] [Aesthetics]** Fix the hero search bar contrast: `components/home/hero-consolidated.tsx` line 76 — the search Input uses `bg-white/10 backdrop-blur-sm border-white/20` which makes it nearly invisible against the gray hero background. Text is `text-gray-800` on a semi-transparent white — low contrast. **Fix:** Change to `bg-white border-gray-200 shadow-sm` for a clean, visible search bar. Time: 3 min. **FIXED 2026-04-01:** Changed to `bg-white border-gray-200 shadow-sm` with dark mode variants (`dark:bg-gray-800 dark:border-gray-700`).

- [x] **[QUICK] [SEO]** Fix the typo in broker page E-E-A-T section: `app/broker/[slug]/page.tsx` line ~108 — "registrios" should be "registries" in the methodology description. This is visible to users AND search engines. Time: 1 min. **FIXED 2026-04-01:** "registrios" → "registries".

- [x] **[QUICK] [UX]** Add `loading="lazy"` to below-fold images: Category Winners (`components/home/category-winners.tsx`) and Featured Brokers (`components/home/featured-brokers.tsx`) render broker logo `<Image>` components without `loading="lazy"`. These are below the fold and should defer. Time: 5 min. **FIXED 2026-04-01:** Added `loading="lazy"` to both Category Winners and Featured Brokers Image components.

---

## AUDIT SUMMARY

| Area | Verdict |
|------|---------|
| **Homepage Structure** | Overloaded — 17 sections is 2x what competitors use. Needs ruthless prioritization. |
| **Visual Design** | The metallic gradient and glassmorphism cards look like a 2015 corporate presentation. The underlying component design is actually solid — the palette ruins it. |
| **Conversion Funnel** | Quiz is excellent but buried. No exit-intent capture. Too many CTAs competing for attention. |
| **Content Quality** | Blog posts are genuinely excellent — detailed, well-structured, data-driven. Best part of the site. |
| **Credibility** | Fake social proof toasts and stock-photo "broker logos" are serious trust risks. |
| **Mobile Experience** | Functional but cramped by sticky bars. Header is oversized. |
| **Performance** | Good use of `dynamic()` imports and SSR flags. Marquee animation is the main perf offender. |
| **SEO** | Strong structured data, good meta tags. Duplicate FAQ schema is the main issue. |
| **vs Competitors** | BrokerChooser wins on simplicity and quiz-first layout. Forexbrokers.com wins on focused sections and credibility. This site has MORE features than both but converts worse because of visual clutter and quiz burial. |
